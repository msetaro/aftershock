Read-only G8 main phase-3 working-tree review

PASS: independently verified all 170 source moves as R100 and compared every new file byte-for-byte with its HEAD parent blob. No additional engine source changes; vendored libraries, renderer2 source, assembly and resources untouched.

PASS: Make single-object dry runs for native default, clang, ARM cross-prefix, and explicit CXX override select the expected C++ driver and -std=c++20, with C-only warnings absent. Vendor libjpeg dry run remains cc with original -Wimplicit/-Wstrict-prototypes and no C++ standard. Engine link driver remains CXX. No BUILD_CXX references remain. Found two residual rend2 directory-creation lines, notified root, and independently confirmed they were removed; no rend2/renderer2 strings remain in Makefile.

PASS: all five engine MSVC projects parse as XML; all six configurations each specify stdcpp20, ExceptionHandling=false, RuntimeTypeInfo=false and empty CINTERFACE=. Every ClCompile path exists; matching .filters source sets are exact; CRLF is preserved with no bare LF. Vendor projects unchanged. Renderer2 project and GUID mappings removed from solution. These are structural checks, not an MSVC build claim; CI remains necessary.

PASS: x86 CI matrix entries and matching archive entries removed while x86_64 remains. Renamed ubuntu job dependencies updated. A feature-branch workflow_dispatch cannot reach publishing: create-testing requires refs/heads/main for push or dispatch, and update-release requires a release event. Advisory CXX probe removed since ordinary builds are now strict C++.

Earlier shader include exception remains isolated in c2705708. No new blocker or catalog violation found. Use fresh/clean build outputs after rename to avoid stale C objects and old source paths in .d files. Final fresh matrix and gate results are being produced independently by root.

No repository edits by reviewer.
