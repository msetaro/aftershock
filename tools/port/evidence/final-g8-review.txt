Final read-only G8 audit: 8a7e8ed2..e3c573a9

No new uncataloged retained engine hunks or behavior-changing edits found. Reviewed final 134-file diff (1859 insertions, 1758 deletions), with prior per-module reviews and a fresh whole-diff token deletion/replacement audit. Token report: final-token-deletions.json and final-hunk-token-deletions.json in this directory. All removed/replaced tokens are T1 destination casts, T3 compound/increment expansion, T4 keyword renames, T5 boundary declarations, T14 register removal, or T19 enum hoist. No floating-point operand, operator, literal, or expression ordering was removed or exchanged.

T21: 364 net double casts: 347 native sites + six common.c rint sites + eleven fallback-M_PI sites. The final seventeen preserve entire original argument expressions. All 74 shader_data.c added extern declarations match original names, unsigned-char types, and sizes; deleting only those declarations recreates original bytes exactly (T18). Final T5 scope keeps actual external boundaries and explicitly listed entry typedefs; static VM callbacks, internal syscall typedefs, and static GetRefAPI carry no annotations. GNU ARM/AArch64 __clear_cache prototype remains a matching imported-runtime prototype (T11), supported by prior cross compiler ABI and C-hash tests.

Diff shape: only common.c (38/38 versus 16/16) and snd_dma.c (2/2 versus 1/1) differ under --minimal -w numstat. The 23 ignored lines are mandatory T15 literal/macro lexical separation. A narrowly documented exception to the raw whitespace heuristic is appropriate; no source changes should be added to game the statistic.

G3 harness: current GCC-only -fno-inline-small-functions -fno-inline-functions-called-once -fno-ipa-sra and MinGW-only -Wa,-L match independently tested fixes. Combined flags pass MinGW be_aas_route, snd_mix, be_ai_weap in /tmp/port-review-combined-g3-a8312f6a/results.json. Earlier inlining-pair recheck passed 143 native contexts. Blanket -fno-inline is rejected because it falsely exposes the C++ integer sin template. Current final-controls.log verifies integer/pinned-double positive controls, true float-overload negative, real COFF local function beginning L, and nested same-size-layout negative. Architecture-gated exact local $a/$d/$t/$x mapping metadata filter preserves source symbols and undefined references.

Legacy warning disposition: freeze observed Clang unused-function (unchanged HasFCOM, two diagnostics/config) and varargs (unchanged enum va_start parameter, one C++ diagnostic/config), with counts and limitation recorded; no engine warning fixes.

Existing blockers remain unchanged: sv_client.c exact C object checksum divergence on the permitted enum compound-result cast; win_snd.c C++ GUID references require source adaptations beyond T1-T23, even with supported CINTERFACE. This audit does not claim these blocked files or full C++ builds pass. No repository files edited by reviewer.
