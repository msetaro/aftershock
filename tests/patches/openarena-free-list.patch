--- a/code/game/bg_alloc.c
+++ b/code/game/bg_alloc.c
@@ -165,7 +165,8 @@
   fmn->cookie = FREEMEMCOOKIE;
   fmn->prev = NULL;
   fmn->next = freeHead;
-  freeHead->prev = fmn;
+  if( freeHead )
+    freeHead->prev = fmn;
   freeHead = fmn;
 }
 
